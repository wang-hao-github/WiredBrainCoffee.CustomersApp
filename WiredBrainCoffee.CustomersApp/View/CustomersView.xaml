<UserControl x:Class="WiredBrainCoffee.CustomersApp.View.CustomersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WiredBrainCoffee.CustomersApp.View" 
             xmlns:valueconverter="clr-namespace:WiredBrainCoffee.CustomersApp.ValueConverter"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <valueconverter:NavigationSideToGridColumnConverter x:Key="NavigationSideToGridColumnConv"/>
        <DataTemplate x:Key="CustomerDataTemplate">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding FirstName, ValidatesOnNotifyDataErrors=False}" FontWeight="Bold"/>
                <TextBlock Text="{Binding LastName}" Margin="10 0 0 0"/>
            </StackPanel>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <!--Customer list-->
        <Grid Width="260"  Background="#777" x:Name="customerListGrid" Grid.Column="{Binding  Path=NavigationSide,Converter={StaticResource NavigationSideToGridColumnConv}}" >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" >
                <Button Margin="10" Width="75" Command="{Binding AddCommand}" >
                    <StackPanel Orientation="Horizontal">
                        <Image Source="/Images/add.jpg" Height="18" Margin="0 0 5 0"></Image>
                        <TextBlock Text="Add"></TextBlock>
                    </StackPanel>
                </Button>
                <Button Content="Delete" Width="75" Margin="0 10 10 10" Command="{Binding DeleteCommand}"  
                        CommandParameter="{Binding SelectedCustomer}" />
                <Button Margin="0 10 10 10" Command="{Binding MoveNavigationCommand}">
                    <Image Source="/Images/move.jpg" Height="18" Width="30"/>
                </Button>
            </StackPanel>
            <ListView x:Name="customerListView"
                      ItemsSource="{Binding Customers}" 
                      SelectedItem="{Binding SelectedCustomer,Mode=TwoWay}"
                      ItemTemplate="{StaticResource CustomerDataTemplate}"
                        Grid.Row="1"  Margin="10 0 10 10"/>

        </Grid>
        <!--Customer detail-->
        <!-- 直接使用元素作为数据源绑定
            <StackPanel Grid.Row="2" Grid.Column="1">
                <Label>Firstname:</Label>
                <TextBox Text="{Binding ElementName=customerListView,Path=SelectedItem.Content,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" />
                <Label>Lastname:</Label>
                <TextBox/>
                <CheckBox>Is developer</CheckBox>
            </StackPanel>
        -->
        <StackPanel Grid.Column="1" Visibility="{Binding IsCustomerSelected,Converter={StaticResource BooleanToVisibilityConv}}">
            <Label>Firstname:</Label>
            <TextBox Text="{Binding SelectedCustomer.FirstName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
            <Label>Lastname:</Label>
            <TextBox Text="{Binding SelectedCustomer.LastName,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"/>
            <CheckBox IsChecked="{Binding SelectedCustomer.IsDeveloper,Mode=TwoWay}" >Is developer</CheckBox>
        </StackPanel>
    </Grid>
</UserControl>
